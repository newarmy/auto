extends layout

block content
    .area
        header(class='header') 汽车品牌列表
        ul(class='brand-list')
            each item in brandArr
               li(class="brand-item")
                  a(href="brand/#{item.sohuId}" target="_blank" title="#{item.name}")
                   img(src="#{item.logo}" width=100 height=100)
                   h3 #{item.name}